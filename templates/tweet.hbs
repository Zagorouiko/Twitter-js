<!-- <p>
  {{text}}
</p>
<li>{{#link-to 'replies'}}Replies{{/link-to}}</li>
{{outlet}}

{{#if isReplying}}
<form>
  <div class="form-group">
    <label for="reply">Reply</label>
    {{input id ="reply" class="form-control" value=reply}}
    <button class="btn btn-primary" {{action 'post'}}>Post</button>
  </div>
</form>

{{else}}

<button class="btn btn-primary" {{action 'reply'}}>Reply</button>
{{/if}} -->
